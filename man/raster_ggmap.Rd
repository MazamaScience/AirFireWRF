% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster_map.R
\name{raster_ggmap}
\alias{raster_ggmap}
\alias{raster_ggmap.RasterLayer}
\alias{raster_ggmap.list}
\title{Geographic map of BlueSky model output}
\usage{
raster_ggmap(
  raster,
  index = 1,
  palette = "Greys",
  breaks = c(0, 12, 35, 55, 150, 250, 350, Inf),
  direction = 1,
  title = "PM2.5",
  timezone = "UTC",
  col_state = "black",
  col_county = "gray80",
  col_na = "transparent",
  verbose = TRUE
)

\method{raster_ggmap}{RasterLayer}(
  raster,
  index = 1,
  palette = "Greys",
  breaks = c(0, 12, 35, 55, 150, 250, 350, Inf),
  direction = 1,
  title = "PM2.5",
  timezone = "UTC",
  col_state = "black",
  col_county = "gray80",
  col_na = "transparent",
  verbose = TRUE
)

\method{raster_ggmap}{list}(
  raster,
  index = 1,
  palette = "Greys",
  breaks = c(0, 12, 35, 55, 150, 250, 350, Inf),
  direction = 1,
  title = "PM2.5",
  timezone = "UTC",
  col_state = "black",
  col_county = "gray80",
  col_na = "transparent",
  verbose = TRUE
)
}
\arguments{
\item{raster}{A Raster\* object or a list of Raster\* objects.}

\item{index}{And index into the Raster\* object. See details.}

\item{palette}{The color palette used to map cell values. This must be one
of the palettes available through \code{ggplot2::scale_colour_brewer()}.}

\item{breaks}{The breaks used to map cell values to colors.}

\item{direction}{Numeric. \code{direction = -1} reverses color palette.}

\item{title}{(Optional) A plot title.}

\item{timezone}{Olson timezone in which times will be displayed.}

\item{col_state}{Color of state lines. (use \code{'transparent'} to hide them.)}

\item{col_county}{Color of county lines. (use \code{'transparent'} to hide them.)}

\item{verbose}{Logical to display messages.}
}
\value{
A ggplot object.
}
\description{
Creates a \pkg{ggplot2} plot object from a Raster\*
object. The returned plot object can be plotted or can be enhanced with
additional \pkg{ggplot2} intructions.
}
\details{
If a list of Raster\* objects is provided, a small-multiples
plot is created.

The \code{index} is typically associated with a time-axis or \code{RasterLayer},
e.g \code{index = 1} is the first hour of a model.
}
\section{Methods (by class)}{
\itemize{
\item \code{RasterLayer}: Method for Raster* objects.

\item \code{list}: Multi-threaded method for a list of Raster* objects.
}}

\examples{
\donttest{

# Load model data
raster <- wrf_load(
  localPath = "~/Data/WRF/wrfout_d3-2020071512-f07-0000.nc"
)

raster_ggmap(raster, title="PNW-4km")
}
}
